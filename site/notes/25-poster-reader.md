# 25 · poster, for the reader

**Lens.** Variant 12's wall poster, re-cut for a stranger reading top to bottom: name, hear
it, cast, scene, why, how, measured, not yet, read. Same enamel, lettering and slips.

**Palette.** Haldi, sindoor, neel, kajal, chuna as in 12; gulabi `#E4437F` for playback only.

**Type.** Yatra One display; Khand for controls, slips, diagrams; Eczar body.

**Signature mechanic.** The pinned programme strip, with the Landing · Technical map switch at
its left; it lists whichever view is showing. The scene is one instrument: play all, play one,
five pips, a live status line.

**Tried and removed.** The pacing slider. The "rules" line. Astro-side `?og`. Round 2: the
minting and rendering diagrams; flex-row diagrams with a drawn
return arrow, which drifted (now a 4-column grid; the return path is computed from the gap).

**Foregrounded.** §1 above the fold; §8 as cast and scene; §6.7/§5.5 as the language switch;
§4; §7 as one diagram plus the door to the map; §5; §6; §11.

**Round 2.** Hindi tab plays: four review voices × three lines, real-audio bars, durations
from the decoded file, `indicCaveat` on every card, only guru line 0 marked checked, a 404
degrades to text. "One description, two engines": one tap plays Mentor's English seed, then
Guru's Hindi line. Technical map: the shared component repainted, as a second view
(`#technical`; `:target`/`:has` scripts off, hash-mirrored class scripts on). Grain on the
`?og` card. Height 8,511 → 8,032 px at 1440; 13,497 → 12,182 at 390.
