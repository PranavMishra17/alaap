# 23 — kolam

**Lens.** The threshold kolam at dawn: red-oxide floor, rice-flour dots, one line that loops around every dot without lifting. Every clip is a sikku chain drawn from its own audio. The sticky header is one dot per section; the chain grows a lobe as you read and closes at the foot. "Not there yet" is the unfinished corner: bare rings on the wet floor.

**Palette.** Oxide `#7a2e1f` (floor, everywhere) · Wet `#521d13` (unfinished corner) · Rice `#f4ecd9` (line, dots, text) · Flour `rgba(244,236,217,.74)` (secondary text) · Marigold `#f0a030` (live line, current dot, flags, caveat rules).

**Type.** Gotu, monoline and rounded, for आलाप, all Devanagari and headings. Catamaran for body, utility and the two Tamil words. The name is a drop-word the meaning flows around.

**Signature mechanic.** Each clip is decoded in the browser (autocorrelation pitch, frame loudness) and laid on a dot grid: time across, pitch up, a rest row at the bottom. One closed line: outward over and under the visited dots, the return pass closing each lobe; loop radius is loudness. The hero draws itself once on load. Playing traces the path in marigold with the audio, then comes home. Reduced motion: no dawn draw, the trace steps on `timeupdate`.

**Tried and removed.** The 02-shaped hero (name left, meaning right). Dotted rules above every section. Latin name in the phone header. Single-frame pitch readings.

**Foregrounded.** §1, §8 as the threshold; §4.1 as three mentor lines on one grid; §7 as a dotted flow; §5 as a ledger, caveat beside; §6 in full; §11.
